const { Message } = require('discord.js');
const { EmbedSuccess, EmbedBusiness, EmbedNoPerm } = require('../Messages/Messages');


var n = "confirm";
module.exports = ({
	name : n,
	async execute(msg = new Message()){
		
	},
});